import React from 'react';
import SignOutButton from '../components/SignOutButton';

const Dashboard = () => {
  return (
    <div>
      <SignOutButton />
    </div>
  );
};

export default Dashboard;
